import React from "react";
import s from './CreateStock.module.scss'

const CreateStock = () => {
  return (
    <section className={s.sheet}>
      <div className={s.wrapper}>
        
      </div>
    </section>
  );
};

export default CreateStock;
